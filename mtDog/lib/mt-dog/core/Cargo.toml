[package]
name = "json-core"
version = "0.1.0"
edition = "2021"

[dependencies]
# Add any dependencies specific to the json-core package if needed
risc0-zkp = { version = "0.12", default-features = false }
serde = "1.0"

